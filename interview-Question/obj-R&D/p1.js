// Creation & Access

// Normal
const user = { name: "Om", age: 24 };

// Dot notation
console.log(user.name); // Om

// Bracket notation
console.log(user["age"]); // 24

// Dynamic key
const key = "name";
console.log(user[key]); // Om
